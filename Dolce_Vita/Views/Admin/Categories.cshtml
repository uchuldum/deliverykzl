
@{
    
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script>
        $(document).ready(function () {

            function getcategories(data) {
                $(".item-row").remove();
                if (data.length >= 0) {
                    $.each(data, function (i, item) {
                        $(".item").append(
                            "<ul class='item-row'>" +
                            "<li id='item-cell'>" + item.name +

                            "</li>" +
                            "<li id='item-cell' class = 'delete'>" + "<span class = '" + item.id + "'>" + "Удалить" + "</span>" +

                            "</li>" +
                            "<li id='item-cell' class = 'update'>" + "<span class = '" + item.id + "'>" + "Изменить" + "</span>" +

                            "</li>" +
                            "</ul>"

                        );
                    });
                }
                else { alert("noone"); }
            }
            

            $.ajax({
                url: "http://localhost:49228/Admin/GetCategory/",
                type: "GET",
                contentType: "application/json",
                success: function (data) {
                    getcategories(data);
                },
                error: function () {
                    alert("error");

                }

            });
            $(document).on("click", ".delete", function () {
                var id = $(this).children().attr('class');
                $.ajax({
                    url: "http://localhost:49228/Admin/DelCategory/",
                    type: "POST",
                   
                    contentType: "application/json",
                    data: JSON.stringify({
                        Id: id,
                        Name : ""
                    }),
                    dataType : "json",
                    success: function (response) {
                        getcategories(response);
                    },
                    error: function () {
                        alert("error");
                    },
                    
                });
            });
            $(document).on("click", ".update", function () {
                var id = $(this).children().attr('class');
                console.log(id);
            });
        });
        
    </script>
</head>
<body>

    

    <div class="item">

    </div>


    <div class="headMenu">

        <form method="post" action="~/Admin/AddCategory">
            <div class="divname"><input class="form-control inputCon" type="text" name="Name" placeholder="Название категории" /></div>

            <input type="submit" value="Добавить" />
        </form>
    </div>

    <style>
        .item {
            display: table;
        }

        .item-row {
            display: table-row;
       
            height: 25px;
        }

        #item-cell {
            display: table-cell;
            padding: 0px 30px;
        }
    </style>



    




</body>
</html>

